#include <stdio.h>
/* Code to perform a sum of numbers */

int main(void) {
/* Declare variables */
   int i;
       float sum1, sum2, diff;

        /* First sum */
	    sum1 = 0.0;
	        for (i=1; i<=1000; i++) {
		    sum1 = sum1 + 1.0/i;

		        }

			/* this part of the code set the value of  Sum1 to numbers for 1 to 1000 - increasing values */

			  /* Second sum */
			       sum2 = 0.0;
			            for (i=1000; i>0; i--) {
				         sum2 += 1.0/i;
					    }

					    /* this part of the code set the value of Sum2 to numbers from 1000 to 1 - decreasing valuea */

					        printf(" Sum1=%f\n",sum1);
						    printf(" Sum2=%f\n",sum2);

						        /* print command  shows the value of Sum1 and Sum2 */

							  /* Find the difference */
							       diff = sum2 - sum1;

							            printf(" Difference between the two is %f\n",diff);

								     /* this shows the calculation result - note tha the values should be the same but they are not because the approximation error */
}
